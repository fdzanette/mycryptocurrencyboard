<h1>Add currency</h1>

 <div class="form-content">
    <%= simple_form_for [@crypto] do |f| %>
      <%= f.input :name, as: :text, label: "Comentário" %>
      <%= f.input :price, as: :text, label: "Price paid" %>
      <%= f.input :total, as: :text, label: "Total" %>
      <%= f.submit "Add", class: "btn btn-primary" %>
    <% end %>
</div>

<%= link_to "Back", root_path %>
