const btnDelete = document.querySelectorAll('.glyphicon-remove');

btnDelete.forEach((btn) => {

  btn.addEventListener('click', (event) => {

  function refreshValues(innerHTML) {
    const newProfitLossValue = document.getElementById('profit-loss');
    //const newNetWorthValue = document.getElementById('net-worth');
    newProfitLossValue.innerHTML = innerHTML;
  }
  refreshValues('<%= j render "cryptos/user_portfolio", cryptos: @cryptos %>');
  console.log(btnDelete);
  });
});
